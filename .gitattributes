# Normalise to LF in the repository; check out with native endings.
* text=auto eol=lf

# PowerShell is launched by Windows tooling that is happier with CRLF.
*.ps1 text eol=crlf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
